@model Customer

<div class="card mb-4 shadow-sm">
        <div class="d-flex justify-content-between align-items-center">
            <div class="btn-group">
                <a asp-route="Edit" asp-route-id="@Model.CustomerId">
                @Model.CustomerName</a>
            </div>
         
        </div>
    </div>
</div>